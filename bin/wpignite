#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('ignite', 'ignite a new project in the specified folder')
  .parse(process.argv)