{
  "name": "test-project",
  "version": "0.0.1",
  "description": "NONE",
  "repository": "NONE",
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "watch": "cross-env NODE_ENV=development webpack -d --progress -w --hide-modules",
    "start": "cross-env NODE_ENV=development webpack-dev-server -w",
    "build": "cross-env NODE_ENV=production webpack -p --progress --hide-modules",
    "build-dev": "cross-env NODE_ENV=production webpack --progress --hide-modules",
    "optimize": "cross-env NODE_ENV=production webpack --profile"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions"
  ],
  "devDependencies": {
    "webpack-ignite": "0.5.3"
  }
}